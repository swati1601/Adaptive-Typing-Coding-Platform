services:
  web:
    build: .
    container_name: codearena
    ports:
      - "5000:5000"
    volumes:
      - ./backend/database.db:/app/backend/database.db
    environment:
      - SECRET_KEY=change-this-in-production
      - FLASK_ENV=production
    restart: unless-stopped
